{
  "name": "newproject",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "setup": "npm install && webdriver-manager clean && webdriver-manager update --standalone=false --gecko=false",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cucumber": "^1.3.2",
    "protractor": "^6.0.0",
    "protractor-cucumber-framework": "^6.1.1"
  }
}
